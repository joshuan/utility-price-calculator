{
  "name": "meters-expense-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && node -r dotenv/config ./build/bin.js",
    "build": "esbuild ./src/index.ts --bundle --outfile=./build/func.js --platform=node --target=esnext && esbuild ./src/bin.ts --bundle --outfile=./build/bin.js --platform=node --target=esnext",
    "lint": "prettier . --check",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.5.0",
    "dotenv": "^16.4.5",
    "esbuild": "^0.23.1",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.637.0"
  }
}
